{
    "buildCommand": "apt-get update && apt-get install -y php8.1-pgsql && curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --optimize-autoloader && npm install && npm run build && php artisan config:cache",
    "installCommand": "php -v",
    "outputDirectory": "public",
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "/index.php" }
    ],
    "framework": "laravel",
    "environment": {
        "PHP_VERSION": "8.1"
    }
}
